# Klap V4 FIXED - Render

IMPORTANTE: Arquivos precisam ser minúsculos: main.py e requirements.txt

Deploy:
1. Crie NOVO repo só pro backend: klap-backend-only
2. Suba apenas esses 3 arquivos: main.py, requirements.txt, Dockerfile (todos minúsculos!)
3. Render > New Web Service > Docker > Deploy
